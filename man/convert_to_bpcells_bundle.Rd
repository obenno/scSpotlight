% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_bpcells_backend.R
\name{convert_to_bpcells_bundle}
\alias{convert_to_bpcells_bundle}
\title{Convert a file to a BPCells bundle archive}
\usage{
convert_to_bpcells_bundle(input_file, output_file = NULL)
}
\arguments{
\item{input_file}{Path to an input Seurat \code{.Rds} or \code{.h5ad} file.}

\item{output_file}{Path to the output \code{.zip} bundle file. If \code{NULL}, the
archive is created next to \code{input_file} with the same base name and a
\code{.zip} suffix.}
}
\value{
The normalized output archive path.
}
\description{
Reads a standard Seurat \code{.Rds} or \code{.h5ad} file, converts assay layers to
BPCells-backed storage, writes a portable scSpotlight bundle, and packages it
as a \code{.zip} archive.
}
\details{
The resulting archive contains:
\itemize{
\item a Seurat \code{.Rds} entrypoint saved with relative bundle paths,
\item BPCells layer directories under \verb{supporting/},
\item a \code{manifest.json} file describing the bundle layout,
\item helper files (\code{README.txt}, \code{load_bundle.R}) for loading the bundle in a
plain R session.
}

This is intended for converting exchange formats into a portable BPCells
bundle that can be loaded by scSpotlight or by \code{SeuratObject::LoadSeuratRds()}
from the extracted bundle directory context.

\code{.h5ad} inputs are imported through \code{BPCells::open_matrix_anndata_hdf5()} for
memory-conserving matrix access. The converter reads \code{obs} and \code{obsm} through
\code{rhdf5} without materializing the full matrix in memory.

Existing BPCells-backed layers are preserved. Non-BPCells assay layers are
rewritten into BPCells-backed on-disk storage before the bundle is created.
}
\examples{
\dontrun{
convert_to_bpcells_bundle("pbmc3k.Rds")
convert_to_bpcells_bundle("pbmc3k.h5ad", "pbmc3k-bundle.zip")
}
}
