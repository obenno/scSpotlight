% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_bpcells_backend.R
\name{write_h5ad_scanpy}
\alias{write_h5ad_scanpy}
\title{Write a Scanpy-compatible h5ad file}
\usage{
write_h5ad_scanpy(
  object,
  output_file,
  assay = NULL,
  x_layer = NULL,
  include_raw = TRUE,
  matrix_buffer_size = 16384L,
  matrix_chunk_size = 1024L,
  gzip_level = 6L,
  obsm_chunk_rows = 4096L
)
}
\arguments{
\item{object}{A Seurat object.}

\item{output_file}{Path to the output \code{.h5ad} file.}

\item{assay}{Assay name to export. Defaults to the active assay.}

\item{x_layer}{Layer to write as \code{X}. Defaults to the preferred expression
layer (\code{data} when present, otherwise \code{counts}).}

\item{include_raw}{Whether to write the \code{raw} group when a counts layer is
available and differs from \code{X}.}

\item{matrix_buffer_size}{BPCells write buffer size for sparse matrix export.}

\item{matrix_chunk_size}{BPCells write chunk size for sparse matrix export.}

\item{gzip_level}{Gzip compression level used for matrix and dense array output.}

\item{obsm_chunk_rows}{Chunk row count for dense \code{obsm}/\code{varm} arrays.}
}
\value{
The normalized output \code{.h5ad} path.
}
\description{
Writes the active Seurat assay to a Scanpy-compatible \code{.h5ad} file using the
AnnData on-disk structure. Matrix payloads are streamed with BPCells and the
surrounding AnnData metadata structure is written with \code{rhdf5}.
}
\examples{
\dontrun{
write_h5ad_scanpy(pbmc, "pbmc.h5ad")
}
}
