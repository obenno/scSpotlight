---
phase: "01-runtime-contract-backbone"
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - "tests/testthat/test-development-contract-docs.R"
  - "DEVELOPMENT.md"
autonomous: true
requirements:
  - DOCS-01
  - XFER-05
must_haves:
  truths:
    - "Developer can find the Analysis Mode backend seam rules in `DEVELOPMENT.md`."
    - "Developer can find the browser payload change checklist in `DEVELOPMENT.md`."
    - "Developer can verify documentation stays aligned with payload manifest and tests."
  artifacts:
    - path: "tests/testthat/test-development-contract-docs.R"
      provides: "Documentation guard test for runtime contract sections and message names."
    - path: "DEVELOPMENT.md"
      provides: "Runtime Contract Backbone documentation and payload change checklist."
      contains: "## Runtime Contract Backbone"
  key_links:
    - from: "DEVELOPMENT.md"
      to: "inst/protocol/browser-payload-contracts.json"
      via: "message contract matrix names the manifest as the machine-readable source"
      pattern: 'browser-payload-contracts\.json'
    - from: "DEVELOPMENT.md"
      to: "tests/testthat/test-browser-payload-contracts.R"
      via: "payload change checklist requires paired R producer tests"
      pattern: "test-browser-payload-contracts"
    - from: "DEVELOPMENT.md"
      to: "srcjs/index.test.js"
      via: "payload change checklist requires paired JS consumer tests"
      pattern: 'srcjs/index\.test\.js'
---

## Phase Goal

**As a** developer, **I want to** rely on stable Analysis Mode backend seams and explicit browser-contract rules, **so that** user-facing workflows can build on them safely.

<objective>
Satisfy DOCS-01 and complete XFER-05 by documenting the runtime contract backbone and adding a test that keeps the documentation in place.

Purpose: Later changes to metadata, reduction, expression, PCA, or patch payloads must update tests, manifest, and protocol notes together.
Output: `DEVELOPMENT.md` runtime contract section and documentation guard test.
</objective>

<execution_context>
@/home/xzx/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/xzx/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@AGENTS.md
@DEVELOPMENT.md
@inst/protocol/browser-payload-contracts.json
@tests/testthat/test-browser-payload-contracts.R
@srcjs/index.test.js
@R/fct_bpcells_backend.R
@R/fct_backend_transfer_adapter.R
</context>

## Artifacts this phase produces

| Artifact | Type | Contract |
|---|---|---|
| `tests/testthat/test-development-contract-docs.R` | R test file | Ensures required runtime contract documentation headings and message names stay present. |
| `test_that("DEVELOPMENT documents runtime contract backbone", ...)` | test case | Verifies docs mention helper seams, manifest, paired tests, cache-version behavior, and no mirrored Analysis DuckDB runtime. |
| `DEVELOPMENT.md` section `## Runtime Contract Backbone` | docs section | Explains Analysis helper seams, browser payload contracts, payload change checklist, and runtime source-of-truth boundaries. |
| `DEVELOPMENT.md` subsection `### Analysis Mode backend seams` | docs subsection | Names Seurat/BPCells helper functions and source-of-truth rules. |
| `DEVELOPMENT.md` subsection `### Browser payload contracts` | docs subsection | Names message contracts from `browser-payload-contracts.json`. |
| `DEVELOPMENT.md` subsection `### Payload change checklist` | docs subsection | Requires manifest, R producer tests, JS consumer tests, cache-version behavior, and docs updates. |
| `DEVELOPMENT.md` subsection `### Runtime source-of-truth boundaries` | docs subsection | Documents local path, raw-data, large-matrix, and unsafe runtime-source duplication boundaries. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add documentation guard test for runtime contracts</name>
  <read_first>
    @tests/testthat/test-development-contract-docs.R (create)
    @DEVELOPMENT.md
    @inst/protocol/browser-payload-contracts.json
    @tests/testthat/test-browser-payload-contracts.R
    @srcjs/index.test.js
  </read_first>
  <files>tests/testthat/test-development-contract-docs.R</files>
  <behavior>
    - Test 1: `DEVELOPMENT.md` contains `## Runtime Contract Backbone` and the subsections `### Analysis Mode backend seams`, `### Browser payload contracts`, `### Payload change checklist`, and `### Runtime source-of-truth boundaries`.
    - Test 2: `DEVELOPMENT.md` names `inst/protocol/browser-payload-contracts.json`, `tests/testthat/test-browser-payload-contracts.R`, and `srcjs/index.test.js`.
    - Test 3: `DEVELOPMENT.md` names all message contracts from the manifest: `meta_ready`, `meta_patch_ready`, `reduction_ready`, `reductions_ready`, `pca_ready`, `expr_ready`, `reduction_cached`, and `expr_cached`.
    - Test 4: `DEVELOPMENT.md` includes the exact phrases `paired R producer test`, `paired JS consumer test`, `cache-version behavior`, and `do not reintroduce a mirrored DuckDB runtime for Analysis Mode`.
  </behavior>
  <action>Create `tests/testthat/test-development-contract-docs.R`. Read `inst/protocol/browser-payload-contracts.json` through `jsonlite::fromJSON()` and assert every manifest message name appears in `DEVELOPMENT.md`. Use plain text assertions against `readLines("DEVELOPMENT.md")`; do not snapshot the full file. This is the RED step: the test should fail until Task 2 adds the required documentation, and Task 1 verification must confirm that expected RED failure instead of expecting future-task state.</action>
  <verify>
    <automated>bash -lc 'set +e; pixi run Rscript -e "devtools::test(filter = '\''development-contract-docs'\'')"; status=$?; if [ "$status" -eq 0 ]; then exit 1; fi; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-development-contract-docs.R` exists and reads the manifest rather than listing message names independently.
    - The test asserts all four required headings and all four required phrases listed in this task.
    - The RED verification command succeeds only by observing the expected documentation-test failure before Task 2 updates `DEVELOPMENT.md`.
  </acceptance_criteria>
  <done>Documentation coverage for DOCS-01 has an executable RED guard test ready for Task 2 to make pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document backend seams and payload change rules</name>
  <read_first>
    @DEVELOPMENT.md
    @AGENTS.md
    @.planning/REQUIREMENTS.md
    @inst/protocol/browser-payload-contracts.json
    @R/fct_bpcells_backend.R
    @R/fct_backend_transfer_adapter.R
    @srcjs/index.js
  </read_first>
  <files>DEVELOPMENT.md</files>
  <behavior>
    - Test 1: Docs explain that Analysis Mode metadata, reductions, features, PCA summaries, and expression must flow through Seurat/BPCells helpers and not through a mirrored Analysis DuckDB runtime.
    - Test 2: Docs explain each browser message contract and point readers to the manifest, R producer test, and JS consumer test.
    - Test 3: Docs define the payload change checklist: update manifest, update paired R producer test, update paired JS consumer test, preserve cache-version behavior, update `DEVELOPMENT.md`, and run targeted test commands.
    - Test 4: Docs call out security/performance boundaries for local paths, raw data/large matrix exposure, and unsafe runtime-source duplication.
  </behavior>
  <action>Update `DEVELOPMENT.md` with a new `## Runtime Contract Backbone` section near the existing architecture/protocol notes. Add exactly these subsections: `### Analysis Mode backend seams`, `### Browser payload contracts`, `### Payload change checklist`, and `### Runtime source-of-truth boundaries`. Name the helper functions `get_backend_metadata`, `get_backend_features`, `get_backend_reduction_names`, `get_backend_reduction`, `get_backend_expr`, `get_backend_pca_stdev`, `prepare_backend_metadata_transfer`, `prepare_backend_reduction_transfer`, `write_backend_pca_stdev_transfer`, and `prepare_backend_expression_transfer`. Name the message contracts `meta_ready`, `meta_patch_ready`, `reduction_ready`, `reductions_ready`, `pca_ready`, `expr_ready`, `reduction_cached`, and `expr_cached`. Include the exact required phrase `do not reintroduce a mirrored DuckDB runtime for Analysis Mode`. Include the targeted verification commands from Plans 01 and 02 so a developer changing payloads can run the paired tests.</action>
  <verify>
    <automated>pixi run Rscript -e "devtools::test(filter = 'development-contract-docs|browser-payload-contracts|analysis-backend-contract')"</automated>
    <automated>pixi run npm test -- srcjs/index.test.js srcjs/modules/arrowReader.test.js srcjs/modules/scatter/scatterModel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `DEVELOPMENT.md` contains the required runtime contract headings and exact phrases verified by `test-development-contract-docs.R`.
    - `DEVELOPMENT.md` references the manifest path, R producer test path, JS consumer test path, and targeted commands.
    - The targeted R and JS commands pass.
  </acceptance_criteria>
  <done>DOCS-01 is satisfied with tested `DEVELOPMENT.md` protocol notes for the runtime contract backbone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → developer changes | Developers use `DEVELOPMENT.md` to decide how to modify payload contracts. |
| Manifest/tests → documentation | Docs must remain aligned with machine-readable contracts and test commands. |
| Runtime source boundary | Docs must keep Analysis Seurat/BPCells and Explore DuckDB/Parquet responsibilities separate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Repudiation | Payload contract changes | mitigate | Documentation checklist requires manifest, paired R producer test, paired JS consumer test, cache-version behavior, and docs updates for every payload change. |
| T-01-10 | Information Disclosure | Local path and raw data rules | mitigate | Docs explicitly prohibit browser payloads from exposing local paths, raw matrices, or full datasets. |
| T-01-11 | Tampering | Runtime source-of-truth boundaries | mitigate | Docs state Analysis Mode must use Seurat/BPCells helpers and do not reintroduce a mirrored DuckDB runtime for Analysis Mode. |
| T-01-12 | Denial of Service | Large transfer patterns | mitigate | Docs preserve Arrow IPC, typed arrays, chunked/path-based expression transfer, and cache-version behavior as required contract rules. |
| T-01-SC | Tampering | Package installs | accept | This plan installs no packages and adds only documentation plus tests. |
</threat_model>

<verification>
Run both task verification commands. Confirm `DEVELOPMENT.md` contains no contradictory statement allowing Analysis Mode mirrored DuckDB runtime or browser exposure of local paths.
</verification>

<success_criteria>
- DOCS-01 appears in this plan's `requirements` frontmatter.
- `DEVELOPMENT.md` documents backend seams, payload contracts, payload change checklist, and source-of-truth boundaries.
- Documentation guard tests pass and keep the protocol notes discoverable.
</success_criteria>

<output>
Create `.planning/phases/01-runtime-contract-backbone/01-03-SUMMARY.md` when done.
</output>
